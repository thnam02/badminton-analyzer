# Bundled SMPL topology for WHAM overlay (generated after WHAM body-model install).
# Expected files:
#   smpl_faces.npy          — (F, 3) int32
#   smpl_j_regressor.npy    — (24, V) float64 (optional; enables RTMPose alignment)
